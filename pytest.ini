# pytest.ini
[pytest]
minversion = 6.0
python_files = "test*.py"
norecursedirs = ".git"
             ".tox"
             "venv*"
             "requirements*"
addopts = --verbose
             --strict-markers
             --color=yes
             --tb=short

testpaths = tests
